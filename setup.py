from setuptools import setup, find_packages

setup(
    name="ai-travel-planner",
    version="0.1.0",
    description="AI-powered travel planner",
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],  # handled by requirements.txt
)
